import type { Metadata } from 'next';
import { ContactPage } from '@/components/elevation/contact';
export const metadata: Metadata = { title: 'Contact', description: 'Questions about Scripture, membership, or the study experience? Start a conversation with Elevation Bible Study.' };
export default ContactPage;
